# Tools — index

> One row per system this instance has actually reached, with the route to it and the trap that cost time. The rules that govern every call are in `instructions-tools.md` — read those before acting on any row here.

| System | Reach | Route | Known traps | Last checked |
|---|---|---|---|---|

*Nothing registered yet — this instance was installed {{INSTALL_DATE}}. Add a row the first time a system is reached, with the trap that cost time.*
